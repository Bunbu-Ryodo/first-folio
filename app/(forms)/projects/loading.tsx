import NpmInstall from "@/app/ui/general-ui/npm-install";

export default function Loading() {
  return (
    <main className="h-full container font-light">
      <NpmInstall label="projects"></NpmInstall>
    </main>
  );
}
